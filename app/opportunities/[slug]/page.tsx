import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {getActiveOffers,getOfferBySlug,getConditions,formatDate} from "@/lib/offers";import {slugify} from "@/lib/slugify";const siteUrl="https://benia.vercel.app";type Props={params:Promise<{slug:string}>};export async function generateStaticParams(){const offers=await getActiveOffers();return offers.map(o=>({slug:slugify(o.brand)}));}export async function generateMetadata({params}:Props):Promise<Metadata>{const{slug}=await params;const o=await getOfferBySlug(slug);if(!o)return{title:"Oportunidad no encontrada",robots:{index:false,follow:false}};const description=o.description||`Consulta la oportunidad de ${o.brand} en BENIA.`;return{title:`${o.brand}: oferta de referido en España`,description,alternates:{canonical:`/opportunities/${slug}`},robots:{index:true,follow:true},openGraph:{type:"article",locale:"es_ES",url:`${siteUrl}/opportunities/${slug}`,title:`${o.brand}: oferta de referido en España`,description}};}export default async function OpportunityPage({params}:Props){const{slug}=await params;const o=await getOfferBySlug(slug);if(!o)notFound();const conditions=getConditions(o.conditions);const jsonLd={"@context":"https://schema.org","@graph":[{"@type":"WebPage",name:`${o.brand}: oferta de referido en España`,url:`${siteUrl}/opportunities/${slug}`,inLanguage:"es-ES"},{"@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"BENIA",item:siteUrl},{"@type":"ListItem",position:2,name:"Oportunidades",item:`${siteUrl}/opportunities`},{"@type":"ListItem",position:3,name:o.brand,item:`${siteUrl}/opportunities/${slug}`}]}]};return <main className="detail-page"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><header className="directory-header"><Link className="brand" href="/"><span className="brand-mark">B</span><span>benia</span></Link><Link className="back-link" href="/opportunities">Oportunidades</Link></header><div className="detail-wrap"><nav className="breadcrumb"><Link href="/">BENIA</Link><span>/</span><Link href="/opportunities">Oportunidades</Link><span>/</span><strong>{o.brand}</strong></nav><article className="detail-article"><header className="detail-header"><div className="detail-brand-row"><div className="detail-icon">{o.icon||"✦"}</div><div><p className="category-label">{o.category}</p><h1>{o.brand}: oferta de referido</h1></div></div>{o.description&&<p className="detail-lead">{o.description}</p>}<div className="badges"><span className={o.verified?"verify":"verify pending"}>{o.verified?"✓ Verificado":"Revisar"}</span>{o.score!==null&&<span>BENIA Score: {o.score}/100</span>}{o.source_type&&<span>Fuente: {o.source_type}</span>}</div></header><section className="reward-panel"><p className="eyebrow">RECOMPENSA PUBLICADA</p><strong>{o.reward}</strong><p>Revisa las condiciones actuales del proveedor antes de registrarte.</p><a className="primary-cta" href={o.referral_url} target="_blank" rel="noopener noreferrer nofollow">CONSEGUIR OFERTA <span>↗</span></a></section>{conditions.length>0&&<section className="detail-section"><h2>Condiciones</h2><ol>{conditions.map((c,i)=><li key={i}>{c}</li>)}</ol></section>}<section className="detail-section"><h2>Información</h2><dl className="facts"><div><dt>Marca</dt><dd>{o.brand}</dd></div><div><dt>Categoría</dt><dd>{o.category}</dd></div><div><dt>Recompensa</dt><dd>{o.reward}</dd></div>{o.expires_at&&<div><dt>Finaliza</dt><dd>{formatDate(o.expires_at)}</dd></div>}{o.updated_at&&<div><dt>Actualización</dt><dd>{formatDate(o.updated_at)}</dd></div>}</dl></section><section className="detail-section methodology"><h2>Sobre esta oportunidad</h2><p>BENIA organiza información pública y de programas de referidos para facilitar su consulta. La elegibilidad y las condiciones finales dependen del proveedor.</p><p>Cuando corresponda, BENIA puede recibir una compensación por enlaces de referido permitidos por el programa.</p></section><footer className="detail-footer"><Link href="/opportunities">← Volver a oportunidades</Link></footer></article></div></main>;}
